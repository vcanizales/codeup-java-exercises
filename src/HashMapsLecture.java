public class HashMapsLecture {

    //Key value pairs
    //Think of objects in javascript when thinking about this structure
    //A data structure in which some sort of value is stored in a place called a bucket and is referenced by a key which is typically, not necessarily, a string.
    //Every key must be unique


}
